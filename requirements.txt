# Dependencies for HTML report generation
Jinja2>=3.0.0

# For LangFlow vulnerability detection
packaging>=21.0

# For other scripts, mostly Python standard library:
#   json, subprocess, sys, argparse,
#   concurrent.futures, dataclasses, pathlib